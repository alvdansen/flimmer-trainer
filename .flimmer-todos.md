# Flimmer-Trainer Pending Items

## License
- LICENSE file is MIT (from scaffold) — should be Apache 2.0 per decision log

## CI/CD
- Add `.github/workflows/security.yml` — OAuth lacks workflow scope, do via GitHub web UI

## Code Sync
- Partner LoRA injection fix (`modules.py`) — regex adapter name stripping instead of hardcoded `.default.`
  - Fix is in dimljus-kit already, verify flimmer-trainer has it
  - Bug: `inject_lora_state_dict()` silently skipped all keys when adapter_name != "default"

## Docs
- Verify all Wan 2.2 MoE hyperparameters are marked as experimental/beta
- Style LoRAs DO use anchor words — confirm docs reflect this
